#VIM command
## origin
`:reg`：可以查看vim的寄存器，但我还不太清楚怎么使用的
`v`：可以切换到试图模式，光标所经过的都会被选中
`V`：block视图模式，会选中光标经过的行
`d`：剪切选中的内容
`p`：粘贴（似乎可以选择从什么寄存器粘贴，但我不太会= =
`y`：似乎是复制？不记得了
`qa`：宏录制，存放到寄存器a中，按`q`结束录制。用`@a`来使用这个宏。当然也可以使用如`7@a`来重复执行7次宏a。
`$`：似乎是这个，命令模式下可以跳到行末。当然end/home依旧好用
`G`：跳到文末
`gg`：跳到文首
`J`：将下一个行直接加到这一行，实际操作似乎是删除掉这一行末尾的换行符
## Plugins
### Vundle
这其实是插件管理器，但是用起来有点毛病。可以的一种方式是，从github下载插件，然后使用：
`:PluginInstall`来安装插件。
### NERDTree
`:NERDTree`：打开当前文件所在的文件目录
在NERDTree打开文件树中操作的时候：
`u`：打开当前路径的上一级目录
`t`：进入到当前光标指向的文件夹
`:tabc`：可以关闭一个NERDTree打开的tab
`ctrl+w+w`：可以快速在NERDTree和文档浏览之间切换
`:NERDTreeToggle`：可以打开/关闭文件树的浏览

